# Event Schedule

Returns a dataframe with the event's qualification match schedule

## Usage

``` r
qual_schedule(event_code, int_output = TRUE)
```

## Arguments

- event_code:

  TBA-legal event key

- int_output:

  convert TBA keys (e.g. "frc1712") to int ("1712")?
